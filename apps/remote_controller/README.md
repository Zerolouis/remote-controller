# Remote Controller Windows app

Flutter Windows UI for choosing client/server mode, showing native-core status, pairing, device selection, and session health. Real-time controller processing stays inside `remote_controller_core` and never runs on the Flutter UI thread.
